{
  "scripts": {
    "build:prepare": "./scripts/recGmi2md thoughts",
    "build:gmi": "./scripts/geminize",
    "build": "npm run build:prepare && eleventy && rm ./thoughts/*.md",
    "review": "./scripts/review",
    "serve": "eleventy --serve",
    "clean": "rm -rf ./_site/ ./_site_gemini/",
    "export": "npm run clean && npm run build && npm run build:gmi"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-plugin-rss": "^1.1.1",
    "clean-css": "^5.1.5",
    "html-minifier": "^4.0.0"
  }
}
