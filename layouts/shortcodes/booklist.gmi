# Books{{ range (first 1 (where .Site.Data.favorites.books "current" true)) }}

I'm currently reading {{ .title }} by {{ .author }}.{{ end }}
{{ $bookDates := slice }}{{ range .Site.Data.favorites.books }}{{ $bookDates = $bookDates | append .date }}{{ end }}{{ $bookDates = sort (uniq $bookDates) "" "desc" }}{{ range $bookDates }}{{ $bookDate := . }}
## {{ cond (eq (len .) 0) "No Date" . }}
{{ range where (where $.Site.Data.favorites.books "date" $bookDate) "current" "!=" true }}
=> https://isbnsearch.org/isbn/{{ .isbn }} {{ .title }} by {{ .author }} ({{ .year }})
   {{ if gt .rating 0 }}{{ .rating | lang.FormatNumber 1 }}/10. {{ end }}{{ if .note }}{{ .note }}{{ end }}{{ end }}
{{ end }}
